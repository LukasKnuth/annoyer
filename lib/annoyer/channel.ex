defmodule Annoyer.Channel do
    
    @doc "Imports any necessary modules for simple usage."
    defmacro __using__(_opts) do
        quote do
            import Annoyer.Channel
        end
    end

    def filter(filter) do
        
    end

    def outgoing(outgoing) do
        
    end

end